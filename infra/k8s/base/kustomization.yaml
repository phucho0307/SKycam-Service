apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/part-of: observatory-services

resources:
  - mongodb.yaml
  - api.yaml
  - skycam.yaml
  - release-proxy.yaml
  - frontend.yaml
  - ingress.yaml
  - products.yaml
